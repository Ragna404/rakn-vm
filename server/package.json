{
  "name": "omarchy-vm-server",
  "version": "1.0.0",
  "description": "Backend proxy for GCP Compute Engine API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test server.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.3.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
